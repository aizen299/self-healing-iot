# b1-pod-loss-20260825T163042Z

Generated by `experiments/scripts/summarise.py` from `experiments/results/raw/b1-pod-loss-20260825T163042Z/`. Every number below came out of that run; none is an estimate.

## Setup

| Property | Value |
|---|---|
| Started (UTC) | 2026-08-25T16:30:43Z |
| Failure mode | `pod-loss` |
| Iterations | 20 |
| Devices | 3 — device-001, device-002, device-003 |
| Fleet model | one device per pod (bare Pods, restartPolicy: Never - ADR-010) |
| Tick interval | 1000 ms (publish interval and expected heartbeat interval — one value, ADR-006) |
| Device variant | constrained |
| Device heap | -Xmx64m |
| Device sink | mqtt |
| suspect / offline | 2 / 4 missed heartbeats |
| recovery confirms | 2 heartbeats |
| Machine | macOS-26.5.2-arm64-arm-64bit-Mach-O |

## Recovery success rate

- Failures injected: **20**
- Devices confirmed back online: **20**
- Recovery success rate: **100.0%**
- Operator outcomes: {'REPLACED': 20}

## MTTR — detection to confirmed heartbeats

The gateway's `recoveryDurationMillis`. **This is MTTR.**

| n | min | median | p90 | max | mean |
|---|---|---|---|---|---|
| 20 | 1045 ms | 1399.0 ms | 1761 ms | 2225 ms | 1433.8 ms |

## Operator half — detection to the API server accepting the pod

A *component* of the number above, not a second measurement of the same thing and never to be added to it: it starts at the same instant and ends earlier.

| n | min | median | p90 | max | mean |
|---|---|---|---|---|---|
| 20 | 56 ms | 89.0 ms | 122 ms | 269 ms | 96.7 ms |

## Cross-check — the runner's external view

Wall-clock time from `kubectl delete` returning to the gateway reporting `ONLINE`, polled at 4 Hz from outside the cluster. It should exceed MTTR: it includes the poll interval and the API round trip, and it starts before the gateway has detected anything. Recorded separately so a disagreement with the system's own numbers is visible rather than averaged away.

| n | min | median | p90 | max | mean |
|---|---|---|---|---|---|
| 20 | 1605 ms | 1919.5 ms | 2326 ms | 2899 ms | 2062.7 ms |

## Reading this

This is a recorded result and may be cited as one. It is a measurement of this fleet on this machine at 3 devices, one per pod — not a general claim about Kubernetes recovery, and not comparable to a run at a different device count or fleet model.
