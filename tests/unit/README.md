# tests/unit

Unit tests: telemetry validation, heartbeat logic, failure state
transitions, recovery decision logic, configuration validation.

**Status:** empty until the first module (edge-device) lands in Phase 1.
