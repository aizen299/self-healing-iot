# common

Shared code used across modules: telemetry/event DTOs, wire-format
schema definitions, MQTT/Kafka topic name constants, and small utilities.
Kept deliberately thin — this is not a dumping ground for unrelated
shared logic.

**Status:** not yet implemented (target: introduced alongside Phases 1–2
once the telemetry schema and MQTT topic constants are defined).
