# infrastructure/helm

Optional Helm charts wrapping the Kubernetes manifests in
`infrastructure/kubernetes/`. Only introduced if it simplifies
deployment for later phases (GitOps/ArgoCD) — not required for the core
project.

**Status:** not started (optional, target: Phase 13 if pursued).
