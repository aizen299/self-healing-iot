# Experiment methodology and results (docs)

Narrative writeups of each experiment (hypothesis, setup, result,
conclusion) go here. Raw and processed data live in
`experiments/results/`; scripts and configs live in `experiments/scripts/`
and `experiments/configs/`. See `experiments/README.md` for the
reproducibility contract.

**Status:** no experiments run yet. Evaluation is a cross-cutting
workstream rather than a single phase — Pillar A becomes measurable as
soon as both edge-device variants exist in Phase 1, Pillar B after
Phases 4 and 9, Pillar C once the pipeline carries load. See the root
`README.md` for the pillar-to-phase mapping.
